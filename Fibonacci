同Leftmost Digit

斐波那契数

fn = 1/√5[ ((1+√5)/2)^n - ((1-√5)/2)^n ]

lg(fn) = -1/2*lg√5 + nlg((1+√5)/2) + lg(1-((1-√5)/(1+√5))^n)

由于(1-√5)/(1+√5) < 1
  1-((1-√5)/(1+√5))^n < 1
  lg(1-((1-√5)/(1+√5))^n) > 1
  即为整数部分，可以不计算
  
  double f = -0.5*log10(5.0)+ (double)n*log10((1+sqrt(5.0))/2);
  f -= floor(f);
  f = pow(10.0, f);
  
  f为小数部分
